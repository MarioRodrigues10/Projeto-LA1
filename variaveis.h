/** 
 * \brief função que inicializa o nosso array das Letras com os valores iniciais
 * @param s stack
 */
void iniciarArray(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável A na stack
 * @param s 
 */
void varA(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável B na stack
 * @param s 
 */
void varB(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável C na stack
 * @param s 
 */
void varC(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável D na stack
 * @param s 
 */
void varD(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável E na stack
 * @param s 
 */
void varE(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável F na stack
 * @param s 
 */
void varF(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável G na stack
 * @param s 
 */
void varN(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável H na stack
 * @param s 
 */
void varS(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável I na stack
 * @param s 
 */
void varX(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável J na stack
 * @param s 
 */
void varY(STACK *s);

/**
 * @brief função responsável por colocar o conteúdo da variável K na stack
 * @param s 
 */
void varZ(STACK *s);